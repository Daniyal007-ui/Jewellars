/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['cdn.sanity.io'], // Allow Sanity's CDN for optimized image loading
    },
  };
  
  export default nextConfig;
  